services:
  build:
    image: bose-connect:latest
    build:
      context: .
      dockerfile: Dockerfile
      args:
        USER_ID: ${USER_ID}
        GROUP_ID: ${GROUP_ID}
  app-dev:
    image: bose-connect:latest
    container_name: bose-connect
    volumes:
      - ./:/root/bose-connect
    entrypoint: ping -i 1 localhost
    depends_on:
      - build
